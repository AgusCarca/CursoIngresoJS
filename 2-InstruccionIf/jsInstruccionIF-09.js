function mostrar()
{
// 	var numeroRandom;
//	var max;
//	var min;

//	max = 11
//	min = 0
//	numeroRandom = Math.floor (Math.random() * (max -min)) + min;

//	alert(numeroRandom);


	var numeroRandom;

	numeroRandom = Math.floor (Math.random () * (11 - 0)) + 0;

	alert(numeroRandom);

}